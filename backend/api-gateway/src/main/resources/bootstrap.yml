spring:
  application:
    name: ecommerce-api-gateway

  cloud:
    config:
      uri: http://localhost:8888
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/